use cosmwasm_std::StdError;
use thiserror::Error;

#[derive(Error, Debug, PartialEq)]
pub enum ContractError {
    #[error("{0}")]
    Std(#[from] StdError),
    // this is whatever we want
    #[error("Unauthorized")]
    Unauthorized {},
}
